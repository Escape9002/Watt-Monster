This section is for auto-setup via post-up of interfaces, so that these routes are automaticly added after a reboot!
It should not depend how the interface is named, its autodetected by the ```ip route ``` command
